{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in */src/*.py) d=\"${f%/src/*}\"; [ -f \"$d/Makefile\" ] && make -C \"$d\" build >/dev/null;; esac; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Rebuilding notebooks from source..."
          }
        ]
      }
    ]
  }
}
